import styled from '@emotion/styled';
import ColorSet from '@/styles/colorSet';

const StyledSearchList = {
  Container: styled.div`
    padding: 1rem; .6rem;
    background-color: ${ColorSet['--white']};
  `,
};

export default StyledSearchList;
